<source>
  @type forward
</source>

<match test.**>
  @type secure_forward
  secure no
  self_hostname client
  shared_key hogeposxxx0
  keepalive 30
  enable_strict_verification yes
  <server>
    host localhost
  </server>
  flush_interval 1s
</match>
